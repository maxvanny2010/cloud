spring:
  application:
    name: eureka-server
  cloud:
    config:
      enabled: true
  config:
    import: "optional:configserver:http://localhost:8071,optional:configserver:http://host.docker.internal:8071"
