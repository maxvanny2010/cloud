spring:
  application:
    name: configserver
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
        bootstrap: true
server:
  port: 8888

management:
  endpoints:
    web:
      exposure:
        include: "*"