version: '3.7'
services:
  license:
    image: license
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: "dev"
    networks:
      backend:
        aliases:
          - "license"
networks:
  backend:
    driver: bridge